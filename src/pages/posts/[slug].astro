---
import PostLayout from '../../layouts/PostLayout.astro';
import fs from 'fs';
import path from 'path';
import matter from 'gray-matter';
import { marked } from 'marked';

const { slug } = Astro.params;
const postsDir = path.resolve(process.cwd(), 'src/content/posts');

// 查找匹配的文章
const files = fs.readdirSync(postsDir).filter(file => file.endsWith('.md'));
let matchedFile: string | null = null;

for (const file of files) {
  const fileSlug = file.replace(/\.md$/, '');
  if (fileSlug === slug || fileSlug.toLowerCase() === slug?.toLowerCase()) {
    matchedFile = file;
    break;
  }
}

if (!matchedFile) {
  return Astro.redirect('/404');
}

// 读取文章内容
const filePath = path.join(postsDir, matchedFile);
let fileContent = fs.readFileSync(filePath, 'utf-8');

// 解析 frontmatter，gray-matter 处理
let { data, content } = matter(fileContent);

// 如果 gray-matter 没有剥离成功（content 仍然包含 --- 开头），手动剥离
if (content.startsWith('---')) {
  const lines = fileContent.split('\n');
  let inFrontmatter = false;
  const contentLines = [];
  let foundStart = false;
  let foundEnd = false;

  for (const line of lines) {
    if (!foundStart && line.trim() === '---') {
      foundStart = true;
      inFrontmatter = true;
      continue;
    }
    if (inFrontmatter && line.trim() === '---') {
      inFrontmatter = false;
      foundEnd = true;
      continue;
    }
    if (!inFrontmatter) {
      contentLines.push(line);
    }
  }

  if (foundStart && foundEnd) {
    content = contentLines.join('\n');
  }
}

// 移除微信公众号复制过来末尾的 JS 防复制代码，以及开头的 boilerplate 文本
const lines = content.split('\n');
let cleanedLines = lines.filter(line => {
  const trimmed = line.trim();
  // 过滤掉以 var 开头的 JS 代码行
  if (trimmed.startsWith('var ') || trimmed.startsWith('document.') || trimmed.startsWith('e.')) {
    return false;
  }
  // 过滤掉微信公众号 boilerplate 文本（开头的原创信息和推广链接）
  if (trimmed.startsWith('原创 ') ||
      trimmed.includes('LCX PaintingDiary') ||
      trimmed.includes('在小说阅读器读本章') ||
      trimmed.includes('去阅读') ||
      trimmed.includes('在小说阅读器中沉浸阅读') ||
      trimmed.includes('阅读更多')) {
    return false;
  }
  // 只过滤注释行，保留空行用于段落分隔
  if (trimmed.startsWith('first_sc')) {
    return false;
  }
  return true;
});

// 移除开头连续的空行和残留的 boilerplate
let startIndex = 0;
while (startIndex < cleanedLines.length && cleanedLines[startIndex].trim() === '') {
  startIndex++;
}
while (startIndex < cleanedLines.length) {
  const trimmed = cleanedLines[startIndex].trim();
  if (trimmed === '' || trimmed.startsWith('原创') || trimmed.includes('LCX') || trimmed.includes('小说阅读器')) {
    startIndex++;
  } else {
    break;
  }
}
cleanedLines = cleanedLines.slice(startIndex);

// 如果最后还是有 JS 痕迹，直接截断到最后一个非空行
const lastContentLine = cleanedLines.findLastIndex(line => line.trim() !== '');
if (lastContentLine > 0) {
  cleanedLines = cleanedLines.slice(0, lastContentLine + 1);
}

// 重新连接，保留段落之间的空行
content = cleanedLines.join('\n');

// 格式化日期为 YYYY-MM-DD
let formattedDate: string;
if (data.date instanceof Date) {
  formattedDate = data.date.toISOString().split('T')[0];
} else if (typeof data.date === 'string' && data.date) {
  formattedDate = data.date;
} else if (typeof data.date === 'number') {
  formattedDate = new Date(data.date).toISOString().split('T')[0];
} else {
  // Fallback: use file modification time if date is missing/invalid
  const stat = fs.statSync(filePath);
  formattedDate = new Date(stat.mtime).toISOString().split('T')[0];
}

// 渲染 markdown 为 HTML
const htmlContent = marked.parse(content);

// 更新 data 中的日期为格式化后的
const frontmatter = {
  ...data,
  date: formattedDate,
};

// 如果 markdown 内容已经以一级标题开头，说明文章自带标题，不需要 PostLayout 额外显示 header
// 这样和微信原始文章布局保持一致
const trimmedContent = content.trimStart();
const hasOwnTitle = trimmedContent.startsWith('# ') || trimmedContent.startsWith('## ');
const showHeader = !hasOwnTitle;
---

<PostLayout frontmatter={frontmatter} showHeader={showHeader}>
  <div set:html={htmlContent}></div>
</PostLayout>
