import { defineConfig } from 'astro/config';

export default defineConfig({
  // 部署到 GitHub Pages 时改为：base: '/your-repo-name'
  // 部署到 Vercel 时不需要修改
  site: 'https://david077dng-cpu.github.io',
  base: '/painting-dairy',
});
